# CSR Atari100k requirements — updated for benchmark fork
#
# Original pins: atari-py==0.2.5, gym==0.19.0, numpy==1.21.0, torch==2.0.0
# Updated to work with CUDA 12 + Python 3.10 + ale-py (replaces deprecated atari-py)
#
# Install order:
#   1. conda create -n csr_atari python=3.10 -y
#   2. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   3. pip install -r requirements.txt

# Core ML
einops>=0.3.0
scipy>=1.8

# Env + rendering
gym>=0.25.0,<0.27.0
ale-py==0.11.2
shimmy[gym-v21]>=2.0.0
opencv-python-headless>=4.7
numpy<2

# Config + logging
ruamel.yaml==0.17.4
tensorboard>=2.16.0
tqdm>=4.60
matplotlib>=3.5
pandas>=1.2

# Utilities
cloudpickle>=1.6
imageio>=2.34
moviepy>=1.0
